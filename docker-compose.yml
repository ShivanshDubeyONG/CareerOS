version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: CareerOS
      POSTGRES_PASSWORD: CareerOS
      POSTGRES_DB: CareerOS
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
